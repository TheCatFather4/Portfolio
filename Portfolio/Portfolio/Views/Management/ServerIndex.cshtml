@model ServerList
@{
    Layout = "_CafeLayout";
    ViewData["Title"] = "Server Manager";
}

<div class="container">
    <div class="row" id="titleSection">
        <div class="text-center">
            <h1 class="my-4">Server Manager</h1>
        </div>
        <div class="text-center">
            <h5 class="mb-3">Click on the corresponding <b class="text-info">Edit</b> link to manage a server.</h5>
            <h5 class="mb-3">Hired a new server? Go to the <a asp-controller="Management" asp-action="AddServer" class="btn btn-success mx-2">Add Server</a> portal.</h5>
        </div>
    </div>
    <div class="row">
        <table class="table table-dark table-striped table-hover table-bordered border-primary">
            <thead>
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Date of Birth</th>
                    <th>Hire Date</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var s in Model.Servers)
                {
                    <tr>
                        <td>@s.FirstName</td>
                        <td>@s.LastName</td>
                        <td>@s.DoB.ToShortDateString()</td>
                        <td>@s.HireDate.ToShortDateString()</td>
                        @if (s.TermDate == null)
                        {
                            <td class="text-success">Active</td>
                        }
                        else
                        {
                            <td class="text-danger">Inactive</td>
                        }
                        <td>
                            <a asp-controller="Management" asp-action="EditServer" asp-route-id="@s.ServerID">Edit</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>